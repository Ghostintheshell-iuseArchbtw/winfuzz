# WinFuzz Configuration File
# This file contains default settings for the WinFuzz framework
# Lines starting with # are comments and will be ignored

# Target Configuration
# Specify the target type and parameters
# target_type = api|driver|exe|dll|network
# target_param1 = module/device/path/host:port
# target_param2 = function/export (for API/DLL targets)

target_type = 
target_param1 = 
target_param2 = 
ioctl_code = 0x0

# Directory Configuration
corpus_dir = corpus
crashes_dir = crashes
logs_dir = logs

# Execution Configuration
max_iterations = 1000000
timeout_ms = 5000
threads = 8
max_input_size = 65536

# Coverage Configuration
# coverage_type = none|etw|etw-kernel|intel-pt|lbr
coverage_type = none

# Mutation Configuration
# mutation_strategy = random|deterministic|dictionary|havoc|splice
mutation_strategy = random
dict_file = 
seed_files = 

# Optimization Configuration
minimize_corpus = true
deduplicate_crashes = true

# Interface Configuration
interactive = true
verbose = false

# Advanced Configuration
dry_run = false
